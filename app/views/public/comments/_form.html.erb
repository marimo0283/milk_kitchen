<%= form_with model: [@recipe, @comment], remote: true do |f| %>
  <%= f.text_area :comment, rows: '3', placeholder: "感想、質問などをどうぞ", class: "w-50" %></br>
  <%= f.submit "送信", class: "btn btn-info" %>
<% end %>